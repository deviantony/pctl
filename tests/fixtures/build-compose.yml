version: '3.8'

services:
  app:
    build:
      context: ./test-app
      dockerfile: Dockerfile
    ports:
      - "13000:80"
    restart: unless-stopped

  redis:
    image: redis:alpine
    ports:
      - "16379:6379"
    restart: unless-stopped
