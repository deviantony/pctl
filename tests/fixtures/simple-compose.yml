version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "18080:80"
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    restart: unless-stopped

  redis:
    image: redis:alpine
    ports:
      - "16379:6379"
    restart: unless-stopped
